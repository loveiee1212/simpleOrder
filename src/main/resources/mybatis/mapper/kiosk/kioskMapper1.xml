<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team2.simpleOrder.dao.kiosk.IKioskDao1">
	<select id="getReviewList" parameterType="String" resultType="review">
		SELECT BD_DATE,OAC_NUM,RV_DATE,RV_TEXT,RV_SCORE
		FROM REVIEW 
		WHERE C_CODE=#{c_code}
		ORDER BY RV_DATE DESC
	</select>
	<select id="getReviewImgList" parameterType="String" resultType="reviewImg">
		SELECT BD_DATE,OAC_NUM,RVIMG_SYSNAME
		FROM REVIEW_IMG
		WHERE C_CODE=#{c_code}
		ORDER BY OAC_NUM DESC
	</select>
	<select id="getRequestList" parameterType="String" resultType="String">
		SELECT RQ_KIND
		FROM REQUEST 
		WHERE C_CODE=#{c_code}
		ORDER BY RQ_NUM;
	</select>
	
</mapper>