<?xml version="1.0" encoding="UTF-8"?>

<!-- Member sql -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team2.simpleOrder.dao.member.ICompanyMemberDao1">
		
		<select id="getCInfo" parameterType="String" resultType="Member">
			SELECT * FROM COMPANY WHERE C_CODE = #{value};
		</select>
	
</mapper>