<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team2.simpleOrder.dao.money.ISalesDao">

<select id="getDaySales" parameterType="hashmap" resultType="hashmap">
	SELECT SUM(PMT_CASH) AS PMT_CASH, SUM(PMT_CARD) AS PMT_CARD, BD_DATE
	FROM PAYMENT_TYPE
	WHERE C_CODE=#{c_code} AND DATE(BD_DATE) BETWEEN #{f_date} AND #{l_date}
	GROUP BY BD_DATE
</select>
</mapper>